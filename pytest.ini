[pytest]
; One configuration for the whole repository so `pytest` at the root runs the backend
; suite and every plugin's own tests together.
asyncio_mode = auto
testpaths = backend/tests plugins
pythonpath = backend
filterwarnings =
    error::DeprecationWarning
    ignore::DeprecationWarning:dns.*
    ignore::DeprecationWarning:httpx.*
